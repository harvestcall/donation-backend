<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Staff Login</title>
  <style nonce="<%= cspNonce %>">
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #f3f4f6;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .login-container {
      background: #fff;
      padding: 2rem 2.5rem;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 400px;
    }
    h2 {
      margin-bottom: 1.5rem;
      text-align: center;
      color: #003366;
    }
    label {
      font-weight: bold;
      display: block;
      margin-bottom: 0.5rem;
      color: #333;
    }
    input {
      width: 100%;
      padding: 0.8rem;
      margin-bottom: 1.2rem;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 1rem;
    }
    button {
      width: 100%;
      padding: 0.9rem;
      background-color: #2E7D32;
      color: white;
      border: none;
      font-size: 1rem;
      border-radius: 8px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    button:hover {
      background-color: #27662a;
    }
    .forgot-link {
      text-align: center;
      margin-top: 1rem;
    }
    .forgot-link a {
      color: #003366;
      text-decoration: none;
      font-size: 0.95rem;
    }
    .forgot-link a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h2>Staff Login</h2>

    <% if (typeof error !== 'undefined') { %>
      <div class="error-message" style="
        background: #ffebee;
        color: #b71c1c;
        padding: 12px;
        border-radius: 8px;
        margin-bottom: 20px;
        text-align: center;
      ">
        <%= error %>
      </div>
    <% } %>

    <!-- Add form novalidate to prevent browser validation -->
<form action="/login" method="POST" novalidate>
  <input type="hidden" name="_csrf" value="<%- csrfToken %>">
  
  <!-- Add autocomplete attributes -->
  <input type="email" name="email" id="email" 
         autocomplete="username" required>
  
  <input type="password" name="password" id="password" 
         autocomplete="current-password" required>
  
  <button type="submit">Login</button>
</form>

<!-- Add script to ensure token is properly set -->
<script nonce="<%- cspNonce %>">
  document.addEventListener('DOMContentLoaded', () => {
    // Verify token is present in form
    const csrfInput = document.querySelector('input[name="_csrf"]');
    if (!csrfInput || !csrfInput.value) {
      console.error('CSRF token missing in form!');
      
      // Attempt to recover from cookie
      const cookieToken = document.cookie.match(/(?:csrf-token=)([^;]*)/);
      if (cookieToken && cookieToken[1]) {
        if (!csrfInput) {
          const form = document.querySelector('form');
          const hiddenInput = document.createElement('input');
          hiddenInput.type = 'hidden';
          hiddenInput.name = '_csrf';
          hiddenInput.value = cookieToken[1];
          form.prepend(hiddenInput);
        } else {
          csrfInput.value = cookieToken[1];
        }
      }
    }
  });
</script>